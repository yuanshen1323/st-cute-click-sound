(功能(){
  “使用严格”;
  让audioCtx = 等于零的;
  功能 getCtx(){
    如果(!audioCtx) audioCtx = 新的 (窗.功能||窗.webkitAudioContext)();
    如果(audioCtx.状态==='suspended') audioCtx.简历();
    返回audioCtx;
  }
  功能 playClick(){
    尝试{
      常数上下文=getCtx(), T=上下文.当前时间;
      常数哟1=上下文.振荡器(), g1=上下文.createGain();
      o1.类型='sine'; o1.频率.时间(1200,T);
      o1.频率.指数(800,T+0.08);
      g1.获得.时间(0.15,T); g1.获得.指数(0.001,T+0.15);
      o1.连接(g1); g1.连接(上下文.目的地); o1.开始(T); o1.停止(T+0.2);
      常数哟2=上下文.振荡器(), g2=上下文.createGain();
      o2.类型='sine'; o2.频率.时间(1800,T+0.04);
      o2.频率.指数(1400,T+0.14);
      g2.获得.时间(0.08,T+0.04); g2.获得.指数(0.001,T+0.2);
      o2.连接(g2); g2.连接(上下文.目的地); o2.开始(T+0.04); o2.停止(T+0.25);
    }抓住(E){}
  }
  功能playSend(){
    尝试{
      常数上下文=getCtx(), T=上下文.当前时间;
      [600,800,1000].遍历((字母表的第六个字母,我)=>{
        常数哟=上下文.振荡器(), G=上下文.createGain();
        哟.类型='sine'; 哟.频率.时间(字母表的第六个字母,T+我*0.06);
        G.获得.时间(0.12,T+我*0.06);
        G.获得.指数(0.001,T+我*0.06+0.15);
        哟.连接(G); G.连接(上下文.目的地); 哟.开始(T+我*0.06); 哟.停止(T+我*0.06+0.2);
      });
    }抓住(E){}
  }
  文档.添加事件监听器('click',功能(E){
    常数 T=E.目标;
    如果(T.比赛('#send_but,#send-btn,[class*="send_but"],[class*="send-btn"]')){playSend();返回;}
    如果(T.标签名==='BUTTON'||T.最近的('button')||T.比赛('[class*="btn"],[role="button"],.menu-item,.list-item')){playClick();}
  });
  控制台.日志('💗 银山学院可爱音效已加载 ✨');
})();
